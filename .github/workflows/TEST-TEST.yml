name: Test

on:
  push:
    branches:
      - main
  schedule:
    - cron: '*/3 * * * *' # Equivalent to Bamboo polling every 180 seconds

jobs:
  Spring:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  Default_Job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

# Note: Bamboo plan permissions and branch management do not have direct equivalents in GitHub Actions.
#       Permissions and branch management should be handled in the repository settings directly.
